services:
  # Base de datos PostgreSQL
  - type: pserv
    name: elorza-database
    env: postgresql
    plan: free
    databaseName: sistema_gestion_agricola
    user: elorza
    region: oregon

  # Aplicación Backend
  - type: web
    name: elorza-backend
    env: python
    region: oregon
    plan: free
    buildCommand: |
      echo "🔨 Instalando dependencias Python..."
      pip install -r requirements.txt
      echo "✅ Build completado - usando archivos de frontend precompilados"
    startCommand: gunicorn run:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: FLASK_ENV
        value: production
      - key: USE_REACT
        value: "true"
      - key: FRONTEND_MODE
        value: "react"
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: elorza-database
          property: connectionString
      - key: WEATHER_API_KEY
        sync: false
    domains:
      - elorza-sistema-agricola.onrender.com
